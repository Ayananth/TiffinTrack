version: "3.9"

services:
  django:
    command: >
      sh -c "python manage.py migrate &&
            gunicorn TiffinTrack.wsgi:application --bind 0.0.0.0:8003 --workers 3 --threads 2 --timeout 120"
